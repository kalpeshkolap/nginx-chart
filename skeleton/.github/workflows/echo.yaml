name: ci
on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run Hello World
        run: echo hello world

      - name: Setup ArgoCD CLI 
        uses: imajeetyadav/argocd-cli@v1
        with:
          version: v2.5.7 # optional 
      - name: argocli cmd
        run: |
          echo ${{ github.server_url }}/${{ github.repository }}
          argocd login argocd.saasdevtoppers.click --username admin --password admin@123
          argocd repo add ${{ github.server_url }}/${{ github.repository }} --password  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  --grpc-web

